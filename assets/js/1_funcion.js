//Declaración de Función
function example(a, b, c) {
    return a + b + c;
}

//Transformación a Función de Expresión
const example = function (a, b, c) { //Suponiendo que no será modificada la variable más adelante
    return a + b + c;
}